---
name: revdeps
description: Use when evaluating whether package architecture results in calls to all its elements
argument-hint: "[directory]"
---

If $ARGUMENTS is provided, use it as the directory. Otherwise ask the user which directory to analyze. Call the `revdeps` MCP tool with that directory and display the result.
