---
name: defuse
description: Use when analyzing variable and data flow for debugging, auditing, or new package development
argument-hint: "[directory]"
---

If $ARGUMENTS is provided, use it as the directory. Otherwise ask the user which directory to analyze. Call the `defuse` MCP tool with that directory and display the result.
